-module(tend).

-export([load/1,
         resume/0,
         set_dir/1,
         get_dir/0,
         reload/0,
         clean_up/0
        ]).


load(_Path) ->
    ok = not_implemented.

resume() ->
    ok = not_implemented.

set_dir(_Dir) ->
    ok = not_implemented.

get_dir() ->
    ok = not_implemented.

reload() ->
    ok = not_implemented.

clean_up() ->
    ok = not_implemented.
